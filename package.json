{
  "name": "kingsman",
  "version": "0.0.0",
  "description": "A blank schematics",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:clean:launch": "npm run build && npm run clean:launch",
    "clean:launch": "npm run clean && npm run launch",
    "clean": "git checkout HEAD -- sandbox && git clean -f -d sandbox",
    "test": "npm run build:clean:launch",
    "launch": "cd sandbox && ng g kingsman:kingsman",
    "link:schematic": "npm link && cd sandbox && npm link kingsman",
    "addprop": "npm run build && cd sandbox && ng g kingsman:edit"
  },
  "keywords": [
    "schematics"
  ],
  "author": "",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^8.3.23",
    "@angular-devkit/schematics": "^8.3.23",
    "@types/jasmine": "^3.3.9",
    "@types/node": "^8.0.31",
    "jasmine": "^3.3.1",
    "typescript": "~3.5.3"
  }
}
