{
    "properties": {
        "name": {
            "type": "string",
            "description": "Name the component to create",
            "default": "defaultName",
            "x-prompt": "What is the name of the component.."
        },
        "titlePage": {
            "type": "string",
            "description": "Page title",
            "default": "Angular Schematics",
            "x-prompt": "Wich title.."
        }
    }
}