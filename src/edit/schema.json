{
    "properties": {
        "prop": {
            "type": "string",
            "description": "Property Name Menu",
            "default": "contact",
            "x-prompt": "Name the menu to add.."
        }
    }
}